/**
 * Updog module.
 * @module updog
 */

/**
 * Generates string literal representation of updog.
 * @param {Object} [configuration] - Optional configuration mapping for updog
 * @return {string} String literal representation of updog
 */
module.exports = function () {
  return 'updog'
}
